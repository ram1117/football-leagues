import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#353535',
      light: '#535353',
    },
    secondary: {
      main: '#F5F5DC',
    },
  },
});

export default theme;
